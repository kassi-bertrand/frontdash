# FrontDash API Environment Variables
# Copy this file to .env and fill in your actual values for local development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=frontdash
DB_USER=your_db_username
DB_PASSWORD=your_db_password

# API Server Configuration
PORT=3000

# Notes:
# - For AWS deployment, these values are automatically populated from db-config.txt
# - For local development, set these to your local PostgreSQL instance
# - Never commit .env file with real credentials to git
